<a href="https://github.com/hexenwail/hexenwail/releases/download/0.8.0-beta.r24/hexenwail-0.8.0-beta.r24-windows-x86_64.zip">![Download Windows build](https://img.shields.io/badge/%E2%AC%87_Windows_x86__64-blue?style=for-the-badge&logo=windows&logoColor=white)</a>
<a href="https://github.com/hexenwail/hexenwail/releases/download/0.8.0-beta.r24/hexenwail-0.8.0-beta.r24-linux-x86_64.zip">![Download Linux build](https://img.shields.io/badge/%E2%AC%87_Linux_x86__64-8A9A5B?style=for-the-badge)</a>
<a href="https://github.com/hexenwail/hexenwail/releases/download/0.8.0-beta.r24/hexenwail-0.8.0-beta.r24.flatpak">![Download Flatpak](https://img.shields.io/badge/%E2%AC%87_Flatpak-4A90D9?style=for-the-badge&logo=flatpak&logoColor=white)</a>

Compiled Hexen II and Portals of Praevus gamecode ships in both zips and loads automatically — see **Compiled gamecode** below for what that means and how to opt out.

### Changes from [0.8.0-beta.r23](https://github.com/hexenwail/hexenwail/releases/tag/0.8.0-beta.r23)

- Moved the Linux user directory to the XDG Base Directory location. A fresh install now writes savegames, configs and screenshots to `$XDG_DATA_HOME/hexen2` — `~/.local/share/hexen2` unless you set that variable — instead of dropping `~/.hexen2` in your home directory (thanks to @Spoike)
  + ❗ **Upgrading changes nothing.** An existing `~/.hexen2` still wins at runtime, so your saves, configs and dropped-in mods stay exactly where they are. Nothing is migrated, moved or deleted, and there is no prompt
  + `$XDG_DATA_HOME` is honoured only when it is an absolute path, as the spec requires; unset, empty and relative values all fall back to `~/.local/share`
  + Missing parent directories (`~/.local`, `~/.local/share`) are created on a fresh account
  + macOS and Haiku keep their own conventions, unchanged. The dedicated server follows the client, so the two cannot disagree about where user data lives
- Fixed a crash to desktop when changing level on a map with rain or snow. Particles outlive a level change, but the world they test themselves against is torn down and not rebuilt until the new map's server info arrives, so any drop or flake still falling asked a question of a world that was not there. Weather is common in Storm over Thyrion, and rain collision is on by default, so this was reachable on an ordinary hub transition
- Fixed a failed map load poisoning the model cache. A load that started and did not finish left its slot marked loaded and empty, so the *next* attempt to enter that map died on `Mod_PointInLeaf: model w/o nodes` — a message that names neither the real failure nor the load it came from, and which arrives one level change after the thing that actually went wrong. The underlying failure is now the one you see. Reported by BloodShot
- Fixed an out-of-bounds read when a live entity references a sprite that reports no frames. The frame index was clamped into a range without first checking the range was non-empty, so frame 0 of an empty sprite was read past the end of the array and the result dereferenced as a pointer. Such a sprite now draws as nothing. Observed on Storm over Thyrion's dungeon, where `models/waterfalls.spr` is a valid 8-frame sprite — the stale model-cache entry behind it is tracked separately and is not fixed here

This rolls up 11 commits since r23. Seven of those are version bumps, documentation, issue-tracker bookkeeping, or dev-only test-harness changes that touch nothing compiled into the engine, and are omitted above.

### Downloads

- **Linux x86_64** — extract and run `glhexen2` from your Hexen II data directory
- **Linux Flatpak** — `flatpak install hexenwail-*.flatpak`, then place game data in `~/.var/app/io.github.hexenwail.hexenwail/.hexen2/data1/`
- **Windows x86_64** — extract anywhere, drop `pak0.pak`/`pak1.pak` into the included `data1/` folder, run `glh2.exe`

Requires game data files (`data1/pak0.pak`, `data1/pak1.pak`).

### Compiled gamecode — included, and already in use

Both zips carry the Hexen II game logic rebuilt from source, with fixes that are
not in Raven's 1997 `progs.dat` (chiefly dropped backpacks silently vanishing in
co-op and deathmatch).

**There is nothing to install.** The engine ships its own copy beside the
executable and prefers it to the one in your Hexen II folder — on Linux and on
Windows alike, straight out of the zip. Nothing is copied and nothing of yours is
overwritten.

- **To see which gamecode you are running**, look for the `Gamecode:` line the
  engine prints when it loads one. It names the exact file and its checksum.
- **To turn it off**, launch with `-vanillaprogs`. The engine then ignores its own
  copy and uses whatever your Hexen II install provides. Nothing is deleted, so it
  is a per-launch decision.
- **To run different gamecode** — a translation, a balance patch, your own build —
  put it under `<userdir>/<gamedir>/` on Linux, which outranks the bundled copy.
  The engine prints `userdir is:` at startup if you are unsure which one you have;
  it is `~/.local/share/hexen2` on a fresh install and `~/.hexen2` if you already
  had one. Windows has no user directory, so replace the files in `gamecode/`
  beside `glh2.exe` instead.

The `gamecode/` folder in the zip is those same three files. On Windows it *is*
the copy the engine loads, since it sits next to `glh2.exe`; on Linux the engine
loads its own from the platform directory and this one is a spare you can install
by hand into a different engine. See `gamecode/README.txt`.

One thing to know:

- **"Vanilla" now means two different things.** A build running our gamecode will
  behave differently from Raven's in ways that are not engine bugs. If you report
  a problem, please paste the `Gamecode:` line, or say whether `-vanillaprogs`
  changes what you see — that is the fastest way to tell whose bug it is, and it
  deletes nothing. This cuts both ways — some reports against older releases will
  have been gamecode issues we had already fixed.

### Reporting a crash

Windows builds report crashes by themselves — there is nothing to install or enable.
If the game dies, a file called `glh2.RPT` appears next to `glh2.exe`.
Attach that file and `qconsole.log` from the same folder to your report.

The Windows build ships with debug symbols, so that report names the source file
and line the game died on — there is no separate debug download to fetch.

To verify a download, fetch `SHA256SUMS` alongside it and run `sha256sum -c SHA256SUMS`.

**Requirements:** OpenGL 4.3 capable GPU (2012+)


**Full Changelog**: https://github.com/hexenwail/hexenwail/compare/0.8.0-beta.r23...0.8.0-beta.r24
